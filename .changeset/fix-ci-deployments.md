---
"abstraxion-dashboard": patch
---

Fix CI deployment pipeline: inline deploy jobs in release workflows to bypass GITHUB_TOKEN limitation, add robust pre.json mode handling, and fix wrangler deploy --message flag error
