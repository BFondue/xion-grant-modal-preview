---
"abstraxion-dashboard": minor
---

Prevent environment-specific builds from running with a `.env` file present. Vite will fall back to the .env file for undefined env vars causing unexpected behavior.
